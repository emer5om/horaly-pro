# Nixpacks configuration for Laravel + React app
[providers]
php = "8.2"
node = "18"

[build]
cmd = "composer install --no-dev --optimize-autoloader && npm ci && npm run build"

[start] 
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"